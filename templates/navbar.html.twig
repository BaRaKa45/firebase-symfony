<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container justify-content-between">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('auth') }}">LogIn<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="{{ path('disconnect') }}">Logout</a>
                </li>
            </ul>

            <ul class="navbar-nav">
                <li class="nav-item">
                    {% if app.user.name is defined %}
                        <a class="nav-link" href="{{ path('profil') }}">{{ app.user.name }}</a>
                    {% elseif app.user.username is defined %}
                        <a class="nav-link" href="{{ path('profil') }}">{{ app.user.username }}</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>